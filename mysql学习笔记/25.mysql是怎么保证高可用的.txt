1.主备延迟的主要来源
	1)备库所在机器性能比主库差
	2)备库压力大(数据分析或者大量的读操作)
	3)大事务（因为事务要执行完成才会写入binlog）
	4)大表的DDL

2.主备切换的可靠性优先策略
	1)判断从库的seconds_behind_master是否小于一个值(5秒或者更小),若果不是，重复第一步
	2)把主库改为只读
	3)判断从库的seconds_behind_master是否是0，直到0为止
	4)吧从库改为可读写模式
	5)业务请求从库

3.如果把上一步的4,5调到最前面，可能会造成主从数据不一致