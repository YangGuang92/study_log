1.mysql5.6之前，mysql只支持单线程复制，所以主库并发高的时候，从库就会出现严重的延时问题

2.mysql5.6之后会开多个线程来执行(一般设置为8-16),并且需要满足两点
	1)不能造成更新覆盖，即更新同一行的两个事务，必须要分给同一个worker
	2)同一个事务不能拆开，必须放在同一个worker中

3.mysql5.6的并行策略是按库区分

4.mysql5.7.22时，用来区分的hash值是按照"库名 + 表名 + 索引名 + 值" 来计算的，如果一个表除了主键还有其他的唯一索引，那么对于每一个索引都要增加一个hash值