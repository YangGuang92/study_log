1.覆盖索引：可以直接用索引中的信息，避免回表 (如居民身份证号和姓名建立索引，通过身份证号查姓名，可以不用回表)

2.最左前缀原则:索引中最左n个字段可以用到索引,也可以是字符串索引的最左 M 个字符(如:name,age索引,搜索 '张%'，可以用到索引)

3.索引下推，在mysql5.6之后，会对通过索引查询的数据进行过滤，减少回表次数(如:name,age索引，先满足name的数据中，在过滤age，来减少回表)