1.对于内存临时表，排序规则使用的是rowid方法，因为在内存中只是根据数据行的位置来访问内存中的数据，不会多访问磁盘的

2.mysql是怎么定位一行数据的？
	对于有主键id的数据，通过主键id来获取数据
	对于没有主键id的，系统会生成一个6字节长度的rowid

3.rand()函数内部逻辑很复杂，尽量少使用

4.正确的取随机行的方法是：
	获取表总数 c
	随机数r = floor(c*rand());(总数乘以0-1随机数然后取整)
	select * from xxx limit r,1;
