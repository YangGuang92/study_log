1.可重复读的一致性视图是根据数据版本来实现的，并不是真正的存了100G数据的快照，
	具体描述就是：以我启动的时刻为准，数据版本在我之前的我认，在我之后的我不认(查询的时候),更新的时候就必须获取当前的数据，而不是数据版本的数据了，这是为了保证其他事务对同一条数据修改不丢失(当前读)

2.在读提交隔离级别下，每一句语句执行前都会重新产生一个新的视图

总结:对于可重复读，查询只承认在事务启动前就已经提交完成的数据
     对于读提交，查询只承认在语句启动前就已经提交完成的数据